import greenfoot.*;

public class Entity extends Sprack {
    public Entity(MainGame scene, SprackGroup group, int x, int y, int z) {
        super(scene, group, x, y, z);
    }
    
    public void tick() {
        super.tick();
    }
}
